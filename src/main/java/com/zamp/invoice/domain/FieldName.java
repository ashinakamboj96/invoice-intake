package com.zamp.invoice.domain;

public enum FieldName {
    VENDOR_NAME,
    INVOICE_NUMBER,
    INVOICE_DATE,
    CURRENCY,
    SUBTOTAL_AMOUNT,
    TAX_AMOUNT,
    TOTAL_AMOUNT,
    DESCRIPTION,
    QUANTITY,
    UNIT_PRICE,
    AMOUNT
}
